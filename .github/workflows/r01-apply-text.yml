name: R0.1 apply text hardening

on:
  push:
    branches:
      - r0.1/complete-hardening
    paths:
      - tools/r01_text.py
      - .github/workflows/r01-apply-text.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09
        with:
          ref: r0.1/complete-hardening
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@4360b52568e2003a75bf9bc1d59f33a8e3fc893c
        with:
          toolchain: stable
          components: rustfmt
      - name: Apply text hardening
        run: |
          python tools/r01_text.py
          cargo fmt --all
      - name: Check workspace
        run: cargo check --workspace --all-targets
      - name: Test layout text regressions
        run: cargo test -p rarog-layout
      - name: Commit applied changes
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          rm tools/r01_text.py .github/workflows/r01-apply-text.yml
          git add -A
          git commit -m 'R0.1: remove quadratic bootstrap text paths'
          git push origin HEAD:r0.1/complete-hardening
