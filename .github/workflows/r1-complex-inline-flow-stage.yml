name: R1 complex inline flow staging

on:
  push:
    branches:
      - r1/complex-inline-flow-stage

permissions:
  contents: write

jobs:
  patch-and-verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09
      - uses: dtolnay/rust-toolchain@4360b52568e2003a75bf9bc1d59f33a8e3fc893c
        with:
          toolchain: stable
          components: rustfmt, clippy
      - name: Apply complex inline retained-flow slice
        run: python tools/r1_complex_inline_flow_patch_v2.py
      - name: Format
        run: cargo fmt --all
      - name: Check workspace
        run: cargo check --workspace --all-targets
      - name: Strict Clippy
        run: cargo clippy --workspace --all-targets -- -D warnings
      - name: Test engine
        run: cargo test -p rarog-engine
      - name: R0.1 correctness
        run: cargo test -p rarog-engine --test r01_correctness
      - name: Commit verified product change
        shell: bash
        run: |
          if git diff --quiet -- crates/rarog-engine/src/lib.rs crates/rarog-engine/tests/r1_inline.rs crates/rarog-engine/tests/r1_inline_fragmentation.rs; then
            echo "Product slice already present"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add crates/rarog-engine/src/lib.rs crates/rarog-engine/tests/r1_inline.rs crates/rarog-engine/tests/r1_inline_fragmentation.rs
          git commit -m "R1: route complex inline updates through retained flow"
          git push origin HEAD:r1/complex-inline-flow-stage
